name: RDP Access

on:
  workflow_dispatch:

jobs:
  rdp:
    runs-on: windows-latest
    timeout-minutes: 360

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Windows RDP User
        run: |
          net user ${{ secrets.RDP_USER }} ${{ secrets.RDP_PASS }} /add
          net localgroup administrators ${{ secrets.RDP_USER }} /add
          net accounts /maxpwage:UNLIMITED
        shell: powershell

      - name: Enable Remote Desktop
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 1
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
        shell: powershell

      - name: Install and Connect Tailscale
        run: |
          curl.exe -fsSL https://pkgs.tailscale.com/stable/tailscale-setup-unsigned.exe -o tailscale-setup.exe
          Start-Process -FilePath "tailscale-setup.exe" -ArgumentList "/quiet" -Wait
          & "C:\Program Files\Tailscale\tailscale.exe" up --authkey=${{ secrets.TS_AUTHKEY }} --hostname=github-rdp-pc
        shell: powershell

      - name: Display Connection Information
        run: |
          $tsIp = & "C:\Program Files\Tailscale\tailscale.exe" ip -4
          Write-Host "===================================================="
          Write-Host "RDP is ready!"
          Write-Host "Tailscale IP: $tsIp"
          Write-Host "Username: ${{ secrets.RDP_USER }}"
          Write-Host "Connect via your Tailscale IP in the Windows App."
          Write-Host "===================================================="
        shell: powershell

      - name: Keep Workflow Alive
        run: |
          $i = 0
          while ($true) {
            Start-Sleep -Seconds 60
            $i++
            Write-Host "Keep-alive tick: $i minutes elapsed"
          }
        shell: powershell
        
